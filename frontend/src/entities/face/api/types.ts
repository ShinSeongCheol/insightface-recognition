export type Face = {
    id: number,
    name: string,
    image_path: string,
    embedding: [],
    has_embedding: boolean,
    normalized_embedding: number,
    created_at: string,
    updated_at: string
}